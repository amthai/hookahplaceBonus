# Тестирование HookahPlace Bonus

## Локальное тестирование

### 1. Запуск приложения
```bash
# Установка зависимостей
npm install

# Генерация QR кода
npm run generate-qr

# Запуск в режиме разработки
npm run dev
```

### 2. Тестирование API

#### Проверка сервера
```bash
curl http://localhost:3000
```

#### Получение QR кода
```bash
curl http://localhost:3000/api/qr-code
```

#### Создание пользователя
```bash
curl -X POST http://localhost:3000/api/user \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "username": "testuser",
    "first_name": "Test",
    "last_name": "User"
  }'
```

#### Отметка посещения
```bash
curl -X POST http://localhost:3000/api/visit \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "qr_code": "HOOKAH_PLACE_QR"
  }'
```

### 3. Тестирование в браузере

1. Откройте `http://localhost:3000`
2. Проверьте отображение интерфейса
3. Протестируйте кнопку "Отметить посещение"
4. Проверьте симуляцию сканирования QR кода

## Тестирование Telegram Mini App

### 1. Настройка тестового бота

1. Создайте тестового бота через @BotFather
2. Получите токен
3. Настройте Web App URL на `https://your-domain.com`

### 2. Тестирование в Telegram

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите на кнопку "Open App" или "Открыть приложение"
4. Протестируйте все функции

### 3. Тестирование функций

#### ✅ Создание пользователя
- [ ] Пользователь автоматически создается при первом входе
- [ ] Данные из Telegram корректно сохраняются
- [ ] Имя пользователя отображается правильно

#### ✅ Отметка посещения
- [ ] Кнопка "Отметить посещение" открывает сканер
- [ ] Симуляция сканирования работает
- [ ] Посещение корректно записывается в базу
- [ ] Нельзя отметить дважды в один день
- [ ] Прогресс-бар обновляется

#### ✅ Программа лояльности
- [ ] При 10 посещениях выдается бонус
- [ ] Счетчик посещений работает правильно
- [ ] Отображается прогресс до следующего бонуса
- [ ] Бонусы отображаются в истории

#### ✅ История
- [ ] История посещений отображается
- [ ] История бонусов отображается
- [ ] Даты форматируются правильно
- [ ] Статус бонусов отображается

## Автоматическое тестирование

### Создание тестового скрипта
```bash
# Создайте файл test-api.sh
nano test-api.sh
```

Содержимое:
```bash
#!/bin/bash

BASE_URL="http://localhost:3000"
USER_ID=1

echo "🧪 Тестирование API HookahPlace Bonus"
echo "======================================"

# Тест 1: Создание пользователя
echo "1. Создание пользователя..."
RESPONSE=$(curl -s -X POST $BASE_URL/api/user \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "username": "testuser",
    "first_name": "Test",
    "last_name": "User"
  }')

echo "Ответ: $RESPONSE"

# Тест 2: Получение данных пользователя
echo "2. Получение данных пользователя..."
RESPONSE=$(curl -s $BASE_URL/api/user/123456789)
echo "Ответ: $RESPONSE"

# Тест 3: Отметка посещения
echo "3. Отметка посещения..."
RESPONSE=$(curl -s -X POST $BASE_URL/api/visit \
  -H "Content-Type: application/json" \
  -d "{
    \"user_id\": $USER_ID,
    \"qr_code\": \"HOOKAH_PLACE_QR\"
  }")

echo "Ответ: $RESPONSE"

# Тест 4: Получение QR кода
echo "4. Получение QR кода..."
RESPONSE=$(curl -s $BASE_URL/api/qr-code)
echo "QR код получен: $(echo $RESPONSE | grep -o '"qr_code_url":"[^"]*"')"

echo "✅ Тестирование завершено"
```

```bash
# Сделайте скрипт исполняемым
chmod +x test-api.sh

# Запустите тесты
./test-api.sh
```

## Тестирование производительности

### Нагрузочное тестирование
```bash
# Установите Apache Bench
sudo apt install apache2-utils

# Тест нагрузки на API
ab -n 100 -c 10 http://localhost:3000/api/qr-code

# Тест создания пользователей
ab -n 50 -c 5 -p user.json -T application/json http://localhost:3000/api/user
```

Создайте файл `user.json`:
```json
{
  "telegram_id": 123456789,
  "username": "testuser",
  "first_name": "Test",
  "last_name": "User"
}
```

## Тестирование безопасности

### 1. Валидация входных данных
```bash
# Тест с невалидным QR кодом
curl -X POST http://localhost:3000/api/visit \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "qr_code": "INVALID_QR"
  }'

# Ожидаемый результат: ошибка 400
```

### 2. Тест дублирования посещений
```bash
# Первое посещение
curl -X POST http://localhost:3000/api/visit \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "qr_code": "HOOKAH_PLACE_QR"
  }'

# Второе посещение в тот же день
curl -X POST http://localhost:3000/api/visit \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "qr_code": "HOOKAH_PLACE_QR"
  }'

# Ожидаемый результат: ошибка 400 "Already visited today"
```

## Тестирование базы данных

### Проверка структуры
```bash
# Установите sqlite3
sudo apt install sqlite3

# Проверьте структуру базы
sqlite3 database.sqlite ".schema"

# Проверьте данные
sqlite3 database.sqlite "SELECT * FROM users;"
sqlite3 database.sqlite "SELECT * FROM visits;"
sqlite3 database.sqlite "SELECT * FROM bonuses;"
```

## Мониторинг в продакшене

### Логи приложения
```bash
# PM2 логи
pm2 logs hookahplace-bonus

# Nginx логи
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### Метрики
- Количество активных пользователей
- Количество посещений в день
- Количество выданных бонусов
- Время отклика API

## Чек-лист перед запуском

### ✅ Функциональность
- [ ] Создание пользователей работает
- [ ] Отметка посещений работает
- [ ] Программа лояльности работает
- [ ] История отображается
- [ ] QR код генерируется

### ✅ Безопасность
- [ ] Валидация QR кодов
- [ ] Защита от дублирования посещений
- [ ] CORS настроен правильно
- [ ] SQL инъекции защищены

### ✅ Производительность
- [ ] API отвечает быстро
- [ ] База данных работает стабильно
- [ ] Нет утечек памяти
- [ ] Логи не засоряют диск

### ✅ Развертывание
- [ ] Приложение запускается
- [ ] HTTPS настроен
- [ ] Домен работает
- [ ] Telegram Bot подключен
- [ ] Резервное копирование настроено
